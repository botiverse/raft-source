-- Intentionally managed by the post-migration lifecycle step. Production must
-- build this index with CREATE INDEX CONCURRENTLY and verify it is valid/ready;
-- the transactional Drizzle migration only advances the schema snapshot.
SELECT 1;
