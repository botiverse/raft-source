/**
 * Non-message brand identifiers (URL / domain fragments).
 * Visible product labels live in the i18n catalog under `brand.*`.
 */
export const RAFT_APP_SERVER_PATH_PREFIX = "app.raft.build/s/" as const;
